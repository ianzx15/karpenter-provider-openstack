---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openstacknodeclasses.karpenter.ianzx15.github.com
spec:
  group: karpenter.ianzx15.github.com
  names:
    kind: OpenStackNodeClass
    listKind: OpenStackNodeClassList
    plural: openstacknodeclasses
    shortNames:
    - osnc
    singular: openstacknodeclass
  scope: Cluster
  versions:
  - name: v1openstack
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              disks:
                description: Disks defines the disks to attach to the provisioned
                  instance.
                items:
                  properties:
                    boot:
                      description: Boot indicates that this is the boot disk.
                      type: boolean
                    sizeGiB:
                      description: SizeGiB is the size of the disk in GiB.
                      format: int32
                      minimum: 10
                      type: integer
                    volumeType:
                      description: VolumeType is the Cinder volume type (e.g., standard,
                        ssd, high-speed).
                      type: string
                  required:
                  - sizeGiB
                  type: object
                maxItems: 10
                type: array
              floatingIP:
                description: FloatingIP indicates whether to assign a floating IP
                  to the instance.
                type: boolean
              imageRef:
                description: ImageRef is the OpenStack Glance image ID to use for
                  the instance.
                type: string
              imageSelectorTerms:
                description: ImageSelectorTerms is a list of image selector terms.
                  The terms are ORed.
                items:
                  properties:
                    alias:
                      description: Alias specifies the image name or family in OpenStack
                        Glance.
                      maxLength: 60
                      type: string
                    id:
                      description: ID specifies the exact Glance image ID to use.
                      maxLength: 160
                      type: string
                  type: object
                maxItems: 30
                minItems: 1
                type: array
              keyPair:
                description: KeyPair is the OpenStack key pair name to assign to the
                  instance
                type: string
              kubeletConfiguration:
                description: KubeletConfiguration defines args to be used when configuring
                  kubelet on provisioned nodes.
                properties:
                  clusterDNS:
                    description: ClusterDNS is a list of IP addresses for the cluster
                      DNS server.
                    items:
                      type: string
                    type: array
                  cpuCFSQuota:
                    description: CPUCFSQuota enables CPU CFS quota enforcement for
                      containers that specify CPU limits.
                    type: boolean
                  evictionHard:
                    additionalProperties:
                      type: string
                    description: EvictionHard defines hard eviction thresholds.
                    type: object
                  evictionMaxPodGracePeriod:
                    description: EvictionMaxPodGracePeriod is the maximum allowed
                      grace period for terminating pods.
                    format: int32
                    type: integer
                  evictionSoft:
                    additionalProperties:
                      type: string
                    description: EvictionSoft defines soft eviction thresholds.
                    type: object
                  evictionSoftGracePeriod:
                    additionalProperties:
                      type: string
                    description: EvictionSoftGracePeriod defines grace periods for
                      soft eviction thresholds.
                    type: object
                  imageGCHighThresholdPercent:
                    description: ImageGCHighThresholdPercent is the disk usage percent
                      after which image GC is always run.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  imageGCLowThresholdPercent:
                    description: ImageGCLowThresholdPercent is the disk usage percent
                      before which image GC is never run.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  kubeReserved:
                    additionalProperties:
                      type: string
                    description: KubeReserved contains resources reserved for Kubernetes
                      system components.
                    type: object
                  maxPods:
                    description: MaxPods is an override for the maximum number of
                      pods that can run on a worker node.
                    format: int32
                    minimum: 0
                    type: integer
                  podsPerCore:
                    description: PodsPerCore is an override for the number of pods
                      per CPU core.
                    format: int32
                    minimum: 0
                    type: integer
                  systemReserved:
                    additionalProperties:
                      type: string
                    description: SystemReserved contains resources reserved for OS
                      system daemons and kernel memory.
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels to be applied on the OpenStack VM instance.
                type: object
              metadata:
                additionalProperties:
                  type: string
                description: Metadata contains key/value pairs to set as instance
                  metadata.
                type: object
              networks:
                description: Networks specifies the OpenStack networks to attach to
                  the instance.
                items:
                  type: string
                minItems: 1
                type: array
              securityGroups:
                description: SecurityGroups specifies the OpenStack security groups
                  to assign to the instance.
                items:
                  type: string
                type: array
              userData:
                description: UserData to be passed to the instance (cloud-init).
                type: string
            required:
            - imageSelectorTerms
            - networks
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
